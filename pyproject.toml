[tool.poetry]
name = "NeoVic2006-project-CLI"
version = "0.2.1"
description = "gendiff working with 2 files"
authors = ["NeoVic2006 <NeoVic2006@gmail.com>"]

packages = [
  { include = "gendiff" },
]



[tool.poetry.dependencies]
python = "^3.6"
prompt = "^0.4.1"

[tool.poetry.dev-dependencies]
flake8 = "^3.8.4"


[tool.poetry.scripts]
gendiff = "gendiff.gendiff_launcher:main"


[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
